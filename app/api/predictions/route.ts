import {z} from 'zod';
import {db,user,siteOwner,originOK} from '@/lib/server';
import {validateEntry,predictionScore,type PredictionEvent,type PredictionEntry} from '@/lib/predictions';
import type {Tournament} from '@/lib/game';
const entry=z.object({teams:z.array(z.string()).max(64),winners:z.record(z.string(),z.string())});
const action=z.discriminatedUnion('action',[
 z.object({action:z.literal('create'),name:z.string().trim().min(1).max(70),kind:z.enum(['qualifiers','bracket']),teams:z.array(z.string()).min(2).max(32),slots:z.number().int().min(2).max(32),lock:z.string().datetime()}),
 z.object({action:z.literal('pick'),id:z.string(),entry}),z.object({action:z.literal('result'),id:z.string(),entry})]);
function weeklyEvents(t:Tournament):PredictionEvent[]{const groups=new Map<string,NonNullable<PredictionEvent['matches']>>();for(const match of t.matches.filter(match=>(match.stage||'seeding')==='seeding')){const week=match.week||'Week 1';const matches=groups.get(week)||[];matches.push({id:match.id,a:match.a,b:match.b,lock:match.lock,winner:match.winner});groups.set(week,matches);}return [...groups].map(([week,matches])=>({id:'prediction:weekly:'+encodeURIComponent(week),name:week+' match predictions',kind:'weekly',teams:[...new Set(matches.flatMap(match=>[match.a,match.b]))],slots:matches.length,lock:matches.map(match=>match.lock).sort()[0],result:[],winners:Object.fromEntries(matches.filter(match=>match.winner).map(match=>[match.id,match.winner!])),matches}));}
async function activeTournament(){const row=await db().prepare('SELECT data FROM tournaments WHERE id = ?').bind('active').first<{data:string}>();return row?JSON.parse(row.data) as Tournament:null;}
export async function GET(r:Request){try{const u=await user(r)||await siteOwner(r);if(!u)return Response.json({error:'Sign in with Discord to view predictions.'},{status:401});const rows=await db().prepare("SELECT data FROM tournaments WHERE id LIKE 'prediction:%' AND id NOT LIKE 'prediction:weekly:%' ORDER BY rowid DESC").all<{data:string}>();const t=await activeTournament();const events=[...(t?weeklyEvents(t):[]),...rows.results.map(row=>JSON.parse(row.data) as PredictionEvent)];const picks:Record<string,PredictionEntry>={};const saved=await db().prepare("SELECT tournament,data FROM entries WHERE user = ? AND tournament LIKE 'prediction:%'").bind(u.id).all<{tournament:string;data:string}>();for(const p of saved.results)picks[p.tournament]=JSON.parse(p.data);return Response.json({events,picks,points:events.reduce((n,e)=>n+(picks[e.id]?predictionScore(e,picks[e.id]):0),0)},{headers:{'Cache-Control':'no-store'}});}catch{return Response.json({error:'Could not load predictions. Try again.'},{status:503});}}
export async function POST(r:Request){try{if(!originOK(r))return Response.json({error:'Forbidden'},{status:403});const owner=await siteOwner(r);const u=await user(r)||owner;if(!u)return Response.json({error:'Sign in to save predictions.'},{status:401});const text=await r.text();if(text.length>20000)throw Error('Request too large.');const a=action.parse(JSON.parse(text));if(a.action==='pick'&&!await user(r))return Response.json({error:'Sign in with Discord to save predictions.'},{status:401});if(a.action!=='pick'&&!owner)return Response.json({error:'Only the owner can manage predictions.'},{status:403});if(a.action==='create'){if(Date.parse(a.lock)<=Date.now())throw Error('Choose a future closing time.');const active=await db().prepare('SELECT data FROM tournaments WHERE id = ?').bind('active').first<{data:string}>();const teams=active?JSON.parse(active.data).teams as {id:string}[]:[];if(new Set(a.teams).size!==a.teams.length||a.teams.some(id=>!teams.some(t=>t.id===id)))throw Error('Choose different teams from your team list.');if(a.kind==='qualifiers'&&a.slots>a.teams.length)throw Error('There cannot be more bracket spots than teams.');if(a.kind==='bracket'&&![2,4,8,16,32].includes(a.teams.length))throw Error('A bracket needs 2, 4, 8, 16, or 32 seeded teams.');const e:PredictionEvent={id:'prediction:'+crypto.randomUUID(),name:a.name,kind:a.kind,teams:a.teams,slots:a.kind==='qualifiers'?a.slots:a.teams.length,lock:a.lock,result:[],winners:{}};await db().prepare('INSERT INTO tournaments (id,data) VALUES (?,?)').bind(e.id,JSON.stringify(e)).run();return Response.json({saved:true});}
 let row=await db().prepare("SELECT data FROM tournaments WHERE id = ? AND id LIKE 'prediction:%'").bind(a.id).first<{data:string}>();if(a.id.startsWith('prediction:weekly:')){const t=await activeTournament();const weekly=t?weeklyEvents(t).find(event=>event.id===a.id):undefined;if(!weekly)throw Error('Weekly prediction group not found.');const data=JSON.stringify(weekly);await db().prepare('INSERT INTO tournaments (id,data) VALUES (?,?) ON CONFLICT(id) DO UPDATE SET data=excluded.data').bind(weekly.id,data).run();row={data};}if(!row)throw Error('Prediction event not found.');const e=JSON.parse(row.data) as PredictionEvent;validateEntry(e,a.entry,Date.now(),a.action==='result');if(a.action==='result'){e.result=a.entry.teams;e.winners=a.entry.winners;const r=await db().prepare('UPDATE tournaments SET data = ? WHERE id = ? AND data = ?').bind(JSON.stringify(e),e.id,row.data).run();if(!r.meta.changes)throw Error('Results changed. Reload and try again.');}else{const old=await db().prepare('SELECT data FROM entries WHERE user = ? AND tournament = ?').bind(u.id,e.id).first<{data:string}>();const result=old?await db().prepare('UPDATE entries SET data = ? WHERE user = ? AND tournament = ? AND data = ? AND EXISTS (SELECT 1 FROM tournaments WHERE id = ? AND data = ?)').bind(JSON.stringify(a.entry),u.id,e.id,old.data,e.id,row.data).run():await db().prepare('INSERT INTO entries (user,tournament,data) SELECT ?,?,? WHERE EXISTS (SELECT 1 FROM tournaments WHERE id = ? AND data = ?) ON CONFLICT(user,tournament) DO NOTHING').bind(u.id,e.id,JSON.stringify(a.entry),e.id,row.data).run();if(!result.meta.changes)throw Error('Predictions changed. Reload and try again.');}return Response.json({saved:true});}catch(e){return Response.json({error:e instanceof z.ZodError?'Check your selections and closing time.':e instanceof Error?e.message:'Could not save.'},{status:400});}}
