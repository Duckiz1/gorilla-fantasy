import type {Team} from '@/lib/game';
export default function VoteBar({a,b,votes}:{a?:Team;b?:Team;votes?:{a:number;b:number}}){const left=votes?.a||0,right=votes?.b||0,total=left+right;const percent=total?Math.round(left/total*100):0;return <div className="vote-meter"><div className="vote-labels"><span>{a?.name} · {percent}%</span><span>{b?.name} · {total?100-percent:0}%</span></div><div className="vote-track" role="img" aria-label={total?`${a?.name}: ${percent} percent, ${b?.name}: ${100-percent} percent. ${total} votes.`:'No votes yet'}>{total>0&&<><span style={{width:`${left/total*100}%`,background:a?.color||'#5D90C4'}}/><span style={{width:`${right/total*100}%`,background:b?.color||'#F65C42'}}/></>}</div><small>{total?`${total} ${total===1?'vote':'votes'} · One vote per player`:'No votes yet — be the first to pick.'}</small></div>;}
